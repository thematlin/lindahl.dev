on:
  push:
    branches:
    - main
jobs:
  build:
    runs-on: ubuntu-latest
  steps:
    - name: Checkout repo
      uses: actions/checkout@v3
    - name: Build repo
      uses: actions/setup-node@v3
      with:
        node-version: 18
    - run: npm ci
    - run: npm run build